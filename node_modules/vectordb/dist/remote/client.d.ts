import { type AxiosResponse } from 'axios';
import { type Table as ArrowTable } from 'apache-arrow';
export declare class HttpLancedbClient {
    private readonly _dbName?;
    private readonly _url;
    private readonly _apiKey;
    constructor(url: string, apiKey: string, _dbName?: string | undefined);
    get uri(): string;
    search(tableName: string, vector: number[], k: number, nprobes: number, refineFactor?: number, columns?: string[], filter?: string): Promise<ArrowTable<any>>;
    /**
     * Sent GET request.
     */
    get(path: string, params?: Record<string, string | number>): Promise<AxiosResponse>;
}
